<%- if profile_signed_in? %>
  <%= button_to 'Logout', destroy_profile_session_path, class: 'btn btn-secondary', method: :delete %>
<%else%>
  <%= button_to 'Login', new_profile_session_path, class: 'btn btn-primary' %>
<%end%>